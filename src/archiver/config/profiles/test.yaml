# Test profile — deterministic, in-memory, no network. (DESIGN.md §11.4)
log_level: WARNING
log_json: false
database_url: "sqlite+aiosqlite:///:memory:"
min_poll_interval_s: 1
max_poll_interval_s: 5
respect_robots: true
enable_html_fallback: false
enable_auth: false
download_media: false
